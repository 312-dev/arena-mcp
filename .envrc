# Are.na personal access token, injected from 1Password via direnv.
# The item lives in the "MCP" vault; adjust the reference if you named it differently.
export ARENA_ACCESS_TOKEN="$(op read 'op://MCP/are.na api key/password')"
